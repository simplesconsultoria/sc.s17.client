Functional test:

    >>> app = layer['app']
    >>> portal = layer['portal']
    >>> request = layer['request']

    >>> from plone.testing.z2 import Browser
    >>> browser = Browser(app)
    >>> portalURL = portal.absolute_url()

    >>> from plone.app.testing import SITE_OWNER_NAME, SITE_OWNER_PASSWORD
    >>> browser.open(portalURL + '/login_form')
    >>> browser.getControl(name='__ac_name').value = SITE_OWNER_NAME
    >>> browser.getControl(name='__ac_password').value = SITE_OWNER_PASSWORD
    >>> browser.getControl(name='submit').click()

    >>> 'You are now logged in' in browser.contents
    True

    >>> browser.getLink('Home').click()
    >>> browser.getLink('Client').click()
    >>> browser.getControl('Title').value = 'Cras porta suscipit libero'
    >>> browser.getControl('Description').value = 'Morbi eu lectus ipsum.'
    >>> browser.getControl('Save').click()
    >>> 'Item created' in browser.contents
    True

    >>> browser.getLink('Cras porta suscipit libero').click()
    >>> 'Morbi eu lectus ipsum.' in browser.contents
    True

    >>> browser.getLink('Project').click()
    >>> browser.getControl('Title').value = 'Quisque et odio nulla'
    >>> browser.getControl('Description').value = 'Curabitur nec elementum massa.'
    >>> browser.getControl('Save').click()
    >>> 'Item created' in browser.contents
    True

    >>> browser.getLink('Cras porta suscipit libero').click()
    >>> browser.getLink('Quisque et odio nulla').click()
    >>> 'Curabitur nec elementum massa.' in browser.contents
    True

